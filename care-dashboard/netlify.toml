[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Override in Netlify UI if your Railway URL differs:
  # VITE_API_URL = "https://verbilabcare-production.up.railway.app"